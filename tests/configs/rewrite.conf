rewrite ^/x $1;
rewrite ^/something$ $other_thing break;
rewrite ^/something$ $http_host/$0 redirect;
rewrite ^/something$ http://$http_host/somewhere redirect;
rewrite ^/something$ http://$http_host/somewhere permanent;
